global:
  message: Welcome to Global Environment Microoservices
logging:
  level:
    root: INFO
management:
  endpoints:
    web:
      exposure:
        include: health, info, refresh
    endpoint:
      health:
        show-details: always
